## 1. 目录与登记机制

- [x] 1.1 建 `docs/design-s/prototypes/`：`README.md`（runbook：双击评审、右上切换器为非基线元素、实现期对照法、与 design-c 的差异——无 parity/baselines）+ 空 `ADJUSTMENTS.md`（表头：日期/文件/偏差/原因）。验证：目录三件存在，README 写明差异段
  - 证据：目录三件齐（README 含与 design-c 差异段/ADJUSTMENTS 表头）

## 2. 原型制作（顺序：收银台 → 订单 → 退款）

- [x] 2.1 `cashier.html` 四态（选套餐/等待支付/支付成功/过期失败），按 design D2：三档 panel、折扣价展示、协议勾选禁用逻辑、二维码+倒计时、notice 三语气、各态出口文案。验证：双击可在四态间切换；词汇仅用既有家族（pill/notice/panel/btn/seg）；金额均 ¥xx.xx
  - 证据：四态齐可切换；金额 MOCK 集中顶部；词汇仅既有家族；内部状态键已避用 success（改 paid）
- [x] 2.2 `orders.html` 订单列表 + 空态，按 design D3：状态 pill 全集（等待支付/已到账/已关闭/退款中/已退款/已冻结）、起止时间含"未开始"、退款入口仅 fulfilled、空态带出口。验证：六种状态行齐备可切换
  - 证据：七行订单（六状态+未开始变体）+ 空态；含已冻结-联系客服出口；summary 四项时长口径
- [x] 2.3 `refund.html` 退款流，按 design D3/D4：秒级折算预览（mock 常量集中在文件顶部）、确认弹层四行 .num、处理中/已退款/三类拒绝态。验证：数字与探索纪要 §11 终版公式一致（含 45 分钟→¥0.03、10 分钟→不可退 两例）
  - 证据：MOCK 按公式实时计算自证 ¥7.76（2400 分×838080 秒÷2592000 秒）；确认弹层四行 num；拒绝两类 + 已到期实现期后端判因（ADJUSTMENTS 登记）
- [x] 2.4 三个原型的 ADJUSTMENTS 登记：切换器非基线、内联样式的共享段来源行号、任何与标准的偏差及原因。验证：每个 HTML 在登记簿有至少一条记录
  - 证据：ADJUSTMENTS 9 条登记：切换器非基线/样式来源/示例价口径/QR 占位/等待态 warn 定档/六状态映射/折算自证/拒绝态口径

- [x] 2.5 （评审补入）`membership.html` 套餐总览：当前档（最高档归属）/时长构成时间线三态/剩余合计/设备额度已绑定数/免费试用态。验证：两态可切换；档位与时长消耗分离表达正确
  - 证据：评审提出"缺套餐对象+设备数放错地方"；D5 拆分两对象；orders 同步瘦身（去掉设备数、加导航）

- [x] 2.6 （评审补入）`console.html` 控制台首页+菜单 IA：5 项导航、进行中横幅、四快捷卡、试用临期态；配套改名包 A-F/H 落地全部原型。验证：两态可切换；validate 通过
  - 证据：D6 三裁定（流程页不占导航/激活码次入口/首页=枢纽）；改名 9 项全原型同步；「控制台」更名单独立项

- [x] 2.7 （评审修订）**到货-激活两段式**：支付成功=到货待激活，用户点「激活」才入队计时。验证：cashier 成功态主按钮改「立即激活」；membership 加待激活区块与汇总拆分（剩余=已激活口径）；orders 仅保留交易状态（评审纠正：激活不进订单，待激活/立即激活只住"我的套餐"）；refund 补"未激活/排队中退款=全额"
  - 证据：决策 #3 修订记录于探索纪要；tier 归属改"已激活行最高档"（囤单不占额度）；「激活」动词解禁（激活码仍禁）；未激活永不过期（默认口径可否决）

- [x] 2.8 （评审补入）**套餐档位化（tier × period）**：tiers/skus 两张配置表、新增套餐零代码、MAX planned 预告占位。验证：cashier 分组渲染 PRO+MAX 预告卡；档名前缀全原型一致；归属规则按 tier 等级序（纪要同步）

- [x] 2.9 （评审修订）选套餐区按竞品案例重排为**双 seg（档位+时长）→ 购买条 → 权益区**，MAX 为 planned 预告 tab。验证：档位/时长切换联动购买条；MAX 态禁用时长并显预告；seg 为既有词汇

- [x] 2.10 （评审补入）`storymap.html` 用户故事地图：五个入口 → 八阶段主线（每阶段挂屏与态、动作、去向）→ 四条典型路径 + 支付分支一览。验证：双击可读；全部屏/态均被主线或路径覆盖

- [x] 2.11 （评审补入）漏项审计与补齐：**红线=C端 零购买动作**（storymap 标注）；补 cashier 态〇「未登录」（登录+回跳）与 `devices.html` 设备管理页；审计表其余项处置（账户页沿用/C端 提示条另批/订单详情行展开/满员拦截实现期）

- [x] 2.12 （评审补入）**订单详情页** `order-detail.html`（六状态）：订单信息全量（含协议留痕）+ 退款进度区块（条件显示）+ 状态相关操作聚合；orders 行内动作列删、统一「详情 ›」行可点；refund 入口上下文化

- [x] 2.13 （评审补入）订单详情「获取发票」动作：弹层（抬头+邮箱）→ 提交；操作区按状态理全（发票仅已支付态可见）。验证：弹层开合正常

- [x] 2.14 （评审补入）`invoice.html` 发票详情页：打印/另存 PDF（@media print 票据本体 A4 适配）、蓝字/红冲两态、票号复制、查验与红冲说明。验证：双击可读、打印预览仅票据本体

## 3. 评审与交付

- [x] 3.1 自查门禁：文案 §13 对照（动词按钮/补救带出口/无内部术语/无"激活"字样于收银台）、词汇对照（无 .b/.strip/success/danger、无新造类）。验证：逐项清单勾选结果记入本任务下方
  - 证据：违禁词扫描全 0：激活/回调/状态机/success/danger/class=b/strip/裸 hex/emoji（3 处内部键 success→paid 后归零）；12 张状态截图入 evidence/ 自检通过
- [x] 3.2 用户评审：三个原型交付过目，收集修改意见回改。验证：用户确认或修改清单清零
- [x] 3.3 按归档流程收尾（validate → 归档 PR，勿 git add openspec/ 整目录）。验证：archive 后 `openspec validate --all` 全绿
