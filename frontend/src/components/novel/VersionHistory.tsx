export default function VersionHistory({ chapterRef }: { chapterRef: string }) {
  return (
    <div className="flex items-center justify-center h-full text-base-content/40 text-sm">
      [VersionHistory: {chapterRef}]
    </div>
  );
}
