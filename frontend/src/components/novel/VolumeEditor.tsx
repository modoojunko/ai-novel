export default function VolumeEditor({ volumeId }: { volumeId: string }) {
  return (
    <div className="flex items-center justify-center h-full text-base-content/40 text-sm">
      [VolumeEditor: {volumeId}]
    </div>
  );
}
