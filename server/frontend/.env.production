# 生产环境 API 地址：CloudBase 云托管后端（静态托管无反代，必须指向完整域名）
# 注意：CI 构建时 VITE_API_BASE 环境变量注入曾失效（产物回退 /api），故直接写死；
# 如需覆盖仍可用 VITE_API_BASE 环境变量（process.env 优先于 .env 文件）
VITE_API_BASE=https://novel-s-server-297265-7-1468883265.sh.run.tcloudbase.com/api
