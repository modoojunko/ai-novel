# 生产环境 API 地址：统一域名 www（前端与 API 同源，/api 由网关按路径分流到云托管后端，
# 免 CORS；曾写死云托管临时域名，2026-09 归位统一域名）。
# 单一事实源是 GitHub Variable TCB_BACKEND_DOMAIN：CI 构建时烘焙进 .env.production.local
# （云端重建读包内 .env 文件、读不到 runner 环境，见 s-server-deploy.yml「构建前端」）；
# 本文件是云端重建的兜底值，改域名时与该 Variable 同步。
# 历史：CI 只用环境变量注入曾失效（产物回退 /api），兜底值必须落盘（PR #146）。
VITE_API_BASE=https://www.awesomenovel.com/api

# ── 备案信息（工信部要求网站底部悬挂；真实号码不入库）──
# 部署时 s-server-deploy.yml 从 GitHub Secrets 内联注入（process.env 优先于本文件）：
#   VITE_BEIAN_ICP          必填：ICP 备案号，如 粤ICP备XXXXXXXXX号（广东主体备案口径）
#   VITE_BEIAN_POLICE       可选：公安备案号，如 粤公网安备 XXXXXXXXXXXXX号
#   VITE_BEIAN_POLICE_LINK  可选：公安查询链接覆盖；缺省按编号数字拼默认查询地址
VITE_BEIAN_ICP=
VITE_BEIAN_POLICE=
VITE_BEIAN_POLICE_LINK=
