# server/requirements.txt — S端 License 服务
# 版本下界沿用 C端 后端风格；前 4 项为既有精确锁定
pyjwt==2.13.0
passlib[bcrypt]==1.7.4
bcrypt==4.2.1
python-dotenv==1.2.2
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
sqlalchemy>=2.0.35
pydantic>=2.9.0
python-jose[cryptography]>=3.3.0
httpx>=0.27.0
alembic>=1.13.0
# PostgreSQL 驱动（生产 DATABASE_URL 指向 CloudBase PG；本地 SQLite 无需）
psycopg2-binary>=2.9.0
# 微信支付 APIv3 社区 SDK（minibear2021/wechatpayv3，社区推荐非官方出品；
# 2.0.3 支持公钥模式 public_key/public_key_id 初始化，2026-09 供应链审计过）
wechatpayv3==2.0.3
