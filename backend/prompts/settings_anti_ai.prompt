你是一位小说设定专家。基于以下故事前提，为这部小说生成反 AI 规则。

故事前提：{premise}

请生成反 AI 规则的 JSON（只输出 JSON，不要其他文字）：
{
  "fatigue_words_zh": {
    "副词": ["突然", "忽然", "显然"],
    "语气词": ["嗯", "啊", "哦"],
    "过渡": ["就在这时", "就在这时突然"]
  },
  "sentence_rules": [
    {
      "pattern": "不是.*而是.*",
      "reason": "避免对比句式",
      "threshold": 2
    }
  ]
}
